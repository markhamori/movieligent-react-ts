import { fireEvent, render, screen } from "@testing-library/react";
import { Favorites } from "../Favorites";

describe('check favorite component', () => { 
  it('it should trigger showFavorites', () => {
    // render(<Favorites />)
  })
 })